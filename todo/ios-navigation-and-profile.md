# iOS Navigation And Profile

- Drop the dedicated profile tab on iOS.
- Make Settings the rightmost tab option instead of only being reachable from the main menu.
- Make the Settings page use the old profile header with avatar picker and username instead of a plain `settings` title.
